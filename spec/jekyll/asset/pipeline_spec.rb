RSpec.describe Jekyll::Asset::Pipeline do
  it "has a version number" do
    expect(Jekyll::Asset::Pipeline::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
